# Module 01: Environment and Tooling

Checklist:

- [ ] Compiler installed (`gcc --version`)
- [ ] Build tool installed (`make --version`)
- [ ] Debugger installed (`gdb --version`)
- [ ] Tracing tools installed (`strace --version`)

Practice:

- Build and run `exercises/01-hello-system/main.c`
- Run with debugger and inspect execution
- Run with `strace` and inspect system calls
